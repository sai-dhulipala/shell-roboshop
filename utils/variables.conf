# Color codes
R="\e[31m"
G="\e[32m"
Y="\e[33m"
N="\e[0m"

# Logs directory
LOGS_DIR="/var/log/shell-roboshop"

# Temp directory
TEMP_DIR="/tmp/shell-roboshop"

# Application user
APP_USER="roboshop"

# Templates directory
TEMPLATES_DIR="$SCRIPT_DIR/templates"

# Component validation mappings
declare -A VALID_SUBCOMPONENTS=(
    [db]="mongodb mysql rabbitmq redis"
    [backend]="cart catalogue dispatch shipping payment user"
    [frontend]=""
)
